{
    "private": true,
    "type": "module",
    "scripts": {
        "start": "dotnet tool restore && dotnet fable watch src --run npx vite",
        "build": "dotnet tool restore && dotnet fable src --run vite build --emptyOutDir",
        "clean": "dotnet fable clean src --yes"
    },
    "dependencies": {
        "bulma": "^0.9.4",
        "date-fns": "^3.2.0",
        "prop-types": "^15.6.0",
        "react": "^18.2.0",
        "react-dom": "^18.2.0",
        "recharts": "^2.10.4",
        "use-sync-external-store": "^1.2.0"
    },
    "devDependencies": {
        "@vitejs/plugin-react": "^4.2.1",
        "mocha": "^10.2.0",
        "sass": "^1.69.7",
        "vite": "^5.0.11"
    }
}
